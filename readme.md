We need to develop a plugin for wordpress just like elementor but with different features.

User flow

Pages > Add Page > Show button EDIT WITH SHA > Clicks on it > OPEN A PAGE BUILDER page like elementor >
The interface should be 2 cols layout like elementor, left col shows CODE BOX to paste HTML, CSS, JS code, the code will be renderes and showed in RIGHT panel > When its is rendered, on the right panel, when user hover mouse on the elements, all the elements must show a EDITING border orange color, when click on it a element, its CSS properties shouls be displayed on left panel, whatever css property is given in code will be displayed in inputs loop so users who dont know CODING can just manipulate the values > Also, the TEXT, content of the elements could be changed in the LEFR panel > Whatever tags, attribites the selected elements got, all things will be displayed in INPUT in left col > SAVE BUTTON above will save eveeyhing just like elementor does > and in front end it should be displayed on page load 


Notes:
UI UX should be like elementor, modern, compact, minimal
Whatever attributes the selected html has its values should be visible in left colr as input
Whatever css propertires of selected html class posses, must be displayed in left col as input so user can manipulate values.
Save click must save all the inputs to their proerp corresponsind positions and save in DB or whatever wordpress does.
JS code could also be added in the code editor on left col (which is displayed as default) everything should be rendered when render button is clicked.